<div style="width: 100%; height: 200px; overflow: auto; white-space: nowrap; font-size: 14px">
    <ul class="list-unstyled list-inline" id="photos">
        <li ng-repeat="item in images">
            <img width="200" id="{{item.Id}}" ng-click="sendDescription(item)" select-image load-image height="150" style="cursor: pointer"  alt="Empty" />
        </li>
    </ul>
</div>
